import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
   selector: 'app-saved-card-summary',
   standalone: true,
   imports: [CommonModule],
   template: `<p>saved-card-summary works!</p>`,
   styles: ``
})
export class SavedCardSummaryComponent {}
