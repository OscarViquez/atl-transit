/** For Light Mode and Dark Mode  */
export type UiModes = 'light' | 'dark';

/** For When passing icons to components */
export type Icon = {
   name: string;
   alt: string;
   path: string;
   url?: string;
};
