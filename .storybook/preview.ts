import { Preview } from '@storybook/angular';

const preview: Preview = {
  parameters: {
    options: {
      storySort: {
        method: '',
        locales: 'en-US'
      },
    },
  },
};

export default preview;